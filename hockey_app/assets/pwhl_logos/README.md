Place PWHL logo PNGs in this folder for the Games tab.

Expected filenames (preferred):
- `BOS.png`
- `MIN.png`
- `MTL.png` (or `MON.png`)
- `NY.png` (or `NYC.png`)
- `OTT.png`
- `TOR.png`

Notes:
- PNG only.
- The Games tab now uses this folder for league `PWHL` and will not fall back to NHL logos.
- If a file is missing, the card will show the team code text instead of an incorrect NHL logo.
